name: Hello World!

on:
    push:
        branches:
            - main

jobs:
    hello_world_job:
        runs-on: ubuntu-22.04
        steps:
            - name: Say hello
              run: |
                echo "Hello World!"
                echo "Now it is:" 
                date --date='@2147483647'
                echo "Directory content:"
                ls -la
